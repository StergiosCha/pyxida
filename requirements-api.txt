# Runtime deps for the FastAPI backend only (Render/production).
# Pipeline-only libs (matplotlib, openpyxl, xlrd, tabulate) are intentionally
# excluded — they are not imported by anything under api/.
fastapi>=0.110
uvicorn[standard]>=0.29
duckdb>=1.0
pydantic>=2.6
pandas>=2.0
# LLM SDK. Required for the σύμβουλος / LLM commentary, because users bring
# their OWN key through the frontend (OpenRouter or OpenAI) — the SDK must be
# present even when the server itself holds no key. Covers both the "openai"
# and "openrouter" backends (OpenRouter is OpenAI-API-compatible).
openai>=1.50
# Only needed if someone uses PYXIDA_LLM_BACKEND=anthropic with a direct
# Anthropic key; OpenRouter already routes to Claude, so this is optional.
# anthropic>=0.40
